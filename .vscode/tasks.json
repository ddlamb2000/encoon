{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and start docker containers (development)",
            "type": "shell",
            "command": "docker compose -f docker-compose.development.yml up --build -d",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        },
        {
            "label": "Start docker containers (sandbox)",
            "type": "shell",
            "command": "docker compose -f docker-compose.sandbox.yml up -d",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        },
    ]
}