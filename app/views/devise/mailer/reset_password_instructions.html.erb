<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <p><%= I18n.t('devise.mailer.hello', :email => @resource.email) %></p>
    <p></p>
    <p><%= I18n.t('devise.mailer.someone_requested') %></p>
    <p><%= link_to I18n.t('devise.mailer.change'), edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %></p>
    <p><%= I18n.t('devise.mailer.please_ignore') %></p>
    <p><%= I18n.t('devise.mailer.password_wont_change') %></p>
  </body>
</html>