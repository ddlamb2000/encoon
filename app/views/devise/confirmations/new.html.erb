<section class="sidebar">
  <article class="article">
    <%= render "links" %>
  </article>
</section>

<section class="content3-4">
  <article class="document">
    <header>
      <h1><%= I18n.t('devise.no_confirmation') %></h1>
    </header>
    <header class="action-header"><%= I18n.t('devise.resend_conf') %></header>
    <%= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f| %>
      <%= devise_error_messages! %>
      <table class="form">
        <tr>
          <td class="header"><%= f.label :email %><span class="required">*</span></td>
          <td class="string"><%= f.text_field :email %></td>
        </tr>
      </table>
      <aside class="actions">
        <center><div class="button"><%= f.submit I18n.t('devise.resend_conf') %></div></center>
      </aside>
    <% end %>
  </article>
</section>