<section id="breadcrumbs">
  <% breadcrumbs = display_breadcrumbs(session[:breadcrumb_table]).html_safe %>
  <% if breadcrumbs.present? and breadcrumbs != "" %> 
    <article class="article">
      <span class="button">
        <span class="hightlighted">
          <a href="#"><%=t 'general.back_to' %></a>
        </span>
      </span>
      <%= breadcrumbs %>
    </article>
  <% end %>
</section>